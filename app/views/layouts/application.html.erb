<!DOCTYPE html>
<html class="<%= theme_classes('bg-gray-100', 'bg-gray-900') %>">
  <head>
    <title><%= content_for(:title) || "Giosa App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
<body class="min-h-screen <%= theme_classes('bg-gray-100 text-black', 'bg-gray-900 text-white') %>" data-controller="notifications" data-action="notification:show@window->notifications#showNotification">
    <div id="flash_messages" class="fixed top-5 right-5 z-50"></div>
    
    <nav class="<%= theme_classes('bg-white border-gray-200', 'bg-gray-800 border-gray-700') %> shadow-sm border-b">
      <div class="w-full px-6 py-7">
        <div class="flex justify-between items-center max-w-full">
          <%= link_to root_path, class: "text-2xl font-bold #{theme_classes('text-gray-800 hover:text-gray-600', 'text-white hover:text-gray-300')} transition-colors" do %>
            GiosaApp
          <% end %>
          <div class="flex space-x-7 items-center">
            <%= link_to t('navigation.home'), root_path, class: "#{theme_classes('text-gray-700 hover:text-gray-900 hover:bg-gray-100', 'text-gray-300 hover:text-white hover:bg-gray-700')} px-3 py-2 rounded-md text-sm font-medium transition-all duration-200" %>
            <%= link_to t('navigation.posts'), posts_path, class: "#{theme_classes('text-gray-700 hover:text-gray-900 hover:bg-gray-100', 'text-gray-300 hover:text-white hover:bg-gray-700')} px-3 py-2 rounded-md text-sm font-medium transition-all duration-200" %>
            <%= link_to t('navigation.new_post'), new_post_path, class: "#{theme_classes('text-gray-700 hover:text-gray-900 hover:bg-gray-100', 'text-gray-300 hover:text-white hover:bg-gray-700')} px-3 py-2 rounded-md text-sm font-medium transition-all duration-200" %>
            
            <!-- Botón de cambio de idioma -->
            <%= button_to toggle_locale_path, method: :post, 
                          title: t('locale.toggle'), 
                          class: "#{theme_classes('bg-blue-100 hover:bg-blue-200 text-blue-700', 'bg-blue-600 hover:bg-blue-500 text-blue-100')} w-12 h-12 rounded-full transition-all duration-300 border-0 flex items-center justify-center text-sm font-bold shadow-sm hover:shadow-md" do %>
              <%= current_locale == :es ? "EN" : "ES" %>
            <% end %>
            
            <!-- Botón de cambio de tema -->
            <%= button_to toggle_theme_path, method: :post, 
                          title: t('theme.toggle'),
                          class: "#{theme_classes('bg-gray-100 hover:bg-gray-200 text-gray-700', 'bg-gray-600 hover:bg-gray-500 text-yellow-300')} w-12 h-12 rounded-full transition-all duration-300 border-0 flex items-center justify-center text-xl shadow-sm hover:shadow-md" do %>
              <%= session[:dark_theme] ? "☀️" : "🌙" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    
    <main class="py-7">
      <%= yield %>
    </main>

    <%# Notification settings disabled - uncomment to re-enable %>
    <%# render "shared/notification_settings" %>
  </body>
</html>
