<div class="container mx-auto px-4 py-8">
  <div class="text-center mb-12">
    <h1 class="text-5xl font-bold <%= theme_classes('text-gray-800', 'text-white') %> mb-4"><%= t('home.welcome') %></h1>
    <p class="text-xl <%= theme_classes('text-gray-600', 'text-gray-300') %> mb-6"><%= t('home.description') %></p>
    <div class="flex justify-center space-x-4">
      <%= link_to t('home.view_all'), posts_path, class: "bg-indigo-500 hover:bg-indigo-600 text-white px-6 py-3 rounded-md transition-colors font-semibold" %>
      <%= link_to t('navigation.new_post'), new_post_path, class: "bg-green-500 hover:bg-green-600 text-white px-6 py-3 rounded-md transition-colors font-semibold" %>
    </div>
  </div>

  <!-- Ejemplo 1: Contador con Stimulus -->
  <div class="<%= theme_classes('bg-white', 'bg-gray-800') %> rounded-lg shadow-md p-6 mb-8" data-controller="counter">
    <h2 class="text-2xl font-semibold mb-4 <%= theme_classes('text-gray-800', 'text-white') %>">Contador Interactivo</h2>
    <div class="flex items-center justify-center space-x-4">
      <button data-action="click->counter#decrement" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md transition-colors">
        -
      </button>
      <span data-counter-target="display" class="text-3xl font-bold min-w-[3rem] text-center <%= theme_classes('text-gray-800', 'text-white') %>">0</span>
      <button data-action="click->counter#increment" class="bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-md transition-colors">
        +
      </button>
    </div>
    <button data-action="click->counter#reset" class="mt-4 bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded-md transition-colors">
      Reset
    </button>
  </div>

  <!-- Ejemplo 2: Toggle con Stimulus -->
  <div class="<%= theme_classes('bg-white', 'bg-gray-800') %> rounded-lg shadow-md p-6 mb-8" data-controller="toggle">
    <h2 class="text-2xl font-semibold mb-4 <%= theme_classes('text-gray-800', 'text-white') %>">Toggle de Contenido</h2>
    <button data-action="click->toggle#toggle" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-md transition-colors mb-4">
      Mostrar/Ocultar Contenido
    </button>
    <div data-toggle-target="content" class="hidden">
      <div class="<%= theme_classes('bg-blue-50 border-blue-400', 'bg-blue-900 border-blue-500') %> border-l-4 p-4">
        <p class="<%= theme_classes('text-blue-700', 'text-blue-300') %>">¡Este contenido se puede mostrar y ocultar usando Stimulus!</p>
        <p class="<%= theme_classes('text-blue-600', 'text-blue-400') %> mt-2">Stimulus hace que sea muy fácil agregar interactividad a tus vistas de Rails.</p>
      </div>
    </div>
  </div>

  <!-- Ejemplo 3: Hello Controller (ya incluido por defecto) -->
  <div class="<%= theme_classes('bg-white', 'bg-gray-800') %> rounded-lg shadow-md p-6" data-controller="hello">
    <h2 class="text-2xl font-semibold mb-4 <%= theme_classes('text-gray-800', 'text-white') %>">Saludo Personalizado</h2>
    <input data-hello-target="name" type="text" placeholder="Ingresa tu nombre" class="border <%= theme_classes('border-gray-300 bg-white text-gray-900', 'border-gray-600 bg-gray-700 text-white') %> rounded-md px-3 py-2 w-full max-w-xs mb-4">
    <button data-action="click->hello#greet" class="bg-purple-500 hover:bg-purple-600 text-white px-4 py-2 rounded-md transition-colors ml-2">
      Saludar
    </button>
    <div data-hello-target="output" class="mt-4 text-lg font-medium text-purple-600"></div>
  </div>
</div>
